#include "stdafx.h"

/*
class graph
{
	void* _manager;

	graph(void* manager) : _manager(manager) 
	{
	}
};

class Scene
{
	Matrix4d camera;

	Scene(const Matrix4d& camera) 
	{
		this->camera = camera; 
	}
};




class C
{
	C& Add(int x)
	{
		// ...
		return *this;
	}
};

C cnt;

cnt.Add(1).Add(2).Add(3).Add(5); 

*/